buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    feature:
      setup:
        preSteps:
        - sh: echo setup
        replace: true
        steps:
        - sh: echo hello world
      prebuild:
        steps:
        - sh: make test
    pullRequest:
      setup:
        steps:
        - sh: echo hello-world
      build:
        steps:
        - sh: make build
